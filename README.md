# Legal NLP: Система для работы с российским законодательством

Комплексная система для сбора, обработки и анализа российских правовых документов с использованием методов NLP, RAG (Retrieval-Augmented Generation) и fine-tuning языковых моделей.

## 📋 Содержание

- [Описание проекта](#описание-проекта)
- [Возможности](#возможности)
- [Архитектура системы](#архитектура-системы)
- [Установка и настройка](#установка-и-настройка)
- [Быстрый старт](#быстрый-старт)
- [Детальное описание компонентов](#детальное-описание-компонентов)
- [Примеры использования](#примеры-использования)
- [Формат данных](#формат-данных)
- [Конфигурация и параметры](#конфигурация-и-параметры)
- [Решение проблем](#решение-проблем)
- [Структура проекта](#структура-проекта)

---

## 🎯 Описание проекта

**Legal NLP** — это полнофункциональная система для работы с российским законодательством, которая включает:

1. **Автоматический сбор** правовых документов из официальных и неофициальных источников
2. **Предобработку и очистку** юридических текстов
3. **Построение RAG-системы** для семантического поиска статей
4. **Fine-tuning языковых моделей** для работы с юридической областью
5. **Валидацию и проверку качества** данных

Система поддерживает работу как на **Apple Silicon** (M1/M2/M3/M4) через MLX, так и на **NVIDIA GPU** через CUDA.

---

## ✨ Возможности

### 🔍 Сбор данных
- **Множественные источники**: zakonrf.info, sudact.ru, actual.pravo.gov.ru
- **Стратегический паттерн**: Легко добавлять новые источники через `BaseStrategy`
- **Динамический скрапинг**: Поддержка Playwright для JavaScript-контента
- **Автоматическая обработка**: Извлечение статей из различных кодексов (ГК, НК, ТК, ЖК, КоАП)

### 📊 Обработка данных
- **Нормализация форматов**: Автоматическое приведение к единому формату ChatML
- **Очистка текста**: Удаление HTML-артефактов, нормализация пробелов
- **Фильтры качества**: Проверка длины, удаление мусора, обнаружение обрывов
- **Дедупликация**: Удаление точных и близких дубликатов
- **Group Split**: Разделение на train/val/test без утечек данных по статьям

### 🧠 RAG система
- **Векторное хранилище**: ChromaDB для эффективного поиска
- **Многоязычные эмбеддинги**: `multilingual-e5-large-instruct`
- **Чанкинг**: Автоматическое разбиение длинных статей
- **Метаданные**: Сохранение URL, названий, кодов кодексов

### 🎓 Fine-tuning
- **Два бэкенда**: MLX для Apple Silicon, CUDA для NVIDIA GPU
- **QLoRA**: Эффективное обучение с 4-bit quantization
- **Автоматическое определение**: Выбор бэкенда в зависимости от платформы
- **Поддержка длинных последовательностей**: Автоматическое разбиение при необходимости

### ✅ Валидация
- **Проверка структуры**: Валидация формата `messages`
- **Обнаружение утечек**: Проверка пересечений между train/test/val
- **Статистика**: Детальная статистика по датасетам

---

## 🏗️ Архитектура системы

```
┌─────────────────────────────────────────────────────────┐
│                    ИСТОЧНИКИ ДАННЫХ                     │
│  zakonrf.info  │  sudact.ru  │  actual.pravo.gov.ru    │
└────────┬───────────┬───────────┬─────────────────────────┘
         │           │           │
         └───────────┴───────────┘
                    │
         ┌──────────▼──────────┐
         │   WEB SCRAPERS      │
         │  (BaseStrategy)     │
         └──────────┬──────────┘
                    │
         ┌──────────▼──────────┐
         │  DATA COLLECTION    │
         │  (raw JSON files)   │
         └──────────┬──────────┘
                    │
         ┌──────────▼──────────┐
         │  PREPROCESSING      │
         │  Pipeline:          │
         │  - Normalize        │
         │  - Clean            │
         │  - Filter           │
         │  - Deduplicate      │
         │  - Split            │
         └──────────┬──────────┘
                    │
         ┌──────────┴──────────┐
         │                     │
    ┌────▼────┐          ┌────▼─────┐
    │   RAG   │          │ FINE-TUNE│
    │ System  │          │ Pipeline  │
    │(ChromaDB)│         │(MLX/CUDA)│
    └────┬────┘          └────┬─────┘
         │                     │
         └──────────┬───────────┘
                    │
         ┌──────────▼──────────┐
         │  LEGAL ASSISTANT    │
         │  (Trained Models)   │
         └─────────────────────┘
```

---

## 🚀 Установка и настройка

### Требования

- **Python**: 3.11 или 3.12
- **Операционная система**: macOS (для MLX) или Linux/Windows с NVIDIA GPU (для CUDA)
- **Память**: Минимум 16GB RAM (рекомендуется 32GB+)
- **Диск**: ~10GB свободного места для моделей и данных

### Установка базовых зависимостей

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd legal_nlp

# Через Makefile (рекомендуется)
make install          # Базовые зависимости
make install-dev      # Зависимости для разработки (включая pytest, ruff, black)

# Или напрямую через pip/uv
uv pip install -r <(uv pip compile pyproject.toml)
# или
pip install -e .
pip install -e ".[dev]"  # Для разработки
```

### Установка зависимостей для fine-tuning

#### Для Apple Silicon (MLX)

```bash
uv pip install -e ".[finetune]" --platform darwin
# или
pip install mlx mlx-lm sentencepiece trl
```

#### Для NVIDIA GPU (CUDA)

```bash
uv pip install -e ".[finetune]" --platform linux
# или
pip install unsloth[cu121-torch240] bitsandbytes sentencepiece trl
```

### Проверка установки

```bash
# Проверка зависимостей
python3 << 'EOF'
import sys
import importlib

deps = {
    'chromadb': 'chromadb',
    'sentence_transformers': 'sentence-transformers',
    'transformers': 'transformers',
    'peft': 'peft',
    'torch': 'torch',
}

print("Проверка зависимостей:")
for module, package in deps.items():
    try:
        importlib.import_module(module)
        print(f"✓ {package}")
    except ImportError:
        print(f"✗ {package} - НЕ УСТАНОВЛЕН")

# Проверка бэкендов
try:
    import mlx
    print("✓ MLX доступен (для Apple Silicon)")
except ImportError:
    print("✗ MLX не установлен")

try:
    import torch
    if torch.cuda.is_available():
        print(f"✓ CUDA доступна (PyTorch {torch.__version__})")
    else:
        print(f"⚠ PyTorch установлен, но CUDA недоступна")
except ImportError:
    print("✗ PyTorch не установлен")
EOF
```

---

## 🏃 Быстрый старт

### 1. Сбор данных

```bash
# Сбор с actual.pravo.gov.ru (официальный источник)
python src/scrapers/smart-scraper.py

# Данные сохранятся в data/raw/
```

### 2. Обработка данных

```bash
# Обработка собранных данных
python src/scrapers/process_dataset.py

# Предобработка для обучения
python scripts/preprocess_sft_pipeline.py \
    --raw-dir data/raw \
    --output-dir data/processed/sft_datasets
```

### 3. Проверка качества

```bash
python scripts/check_health_datasets.py \
    --train data/processed/sft_datasets/train.jsonl \
    --test data/processed/sft_datasets/test.jsonl \
    --val data/processed/sft_datasets/val.jsonl
```

### 4. Построение RAG системы

```bash
# Создание векторной базы данных
python src/legal_rag/rag_system.py --build \
    --articles data/processed/articles_cleaned.json \
    --db_path ./chroma_legal_db

# Поиск статей
python src/legal_rag/rag_system.py --query "Какая статья регулирует трудовые отношения?" \
    --db_path ./chroma_legal_db
```

### 5. Fine-tuning модели

#### На Apple Silicon

```bash
python src/legal_rag/finetune_qlora_4b.py \
    --data data/processed/sft_datasets/train.jsonl \
    --model Vikhrmodels/QVikhr-3-8B-Instruction-MLX_8bit \
    --output models/legal-assistant-mlx \
    --iters 2000 \
    --batch 1 \
    --rank 16 \
    --lr 2e-5
```

#### На NVIDIA GPU

```bash
python src/legal_rag/finetune_qlora_4b.py \
    --data data/processed/sft_datasets/train.jsonl \
    --model mistralai/Mistral-7B-Instruct-v0.3 \
    --output models/legal-assistant-cuda \
    --epochs 3 \
    --batch 4 \
    --rank 64 \
    --lr 2e-4 \
    --gradient_checkpointing
```

---

## 📚 Детальное описание компонентов

### 1. Web Scrapers (`src/scrapers/`)

#### `smart-scraper.py`
Скрапер для официального источника actual.pravo.gov.ru.

**Особенности:**
- Использует Playwright для динамического контента
- Извлекает redid через JavaScript execution
- Поддерживает все основные кодексы (ГК, НК, ТК, ЖК, КоАП)
- Автоматическая обработка ошибок и retry логика

**Использование:**
```bash
python src/scrapers/smart-scraper.py
```

#### `zakonrf.py` и `sudact.py`
Стратегии скрапинга для соответствующих сайтов.

**Архитектура:**
- Наследуются от `BaseStrategy`
- Реализуют `get_article_links()` и `parse_article()`
- Легко расширяются для новых источников

#### `process_dataset.py`
Обработка собранных данных в единый формат.

**Функции:**
- Очистка текста от артефактов
- Извлечение номеров статей
- Формирование вопросно-ответных пар
- Сохранение в формате ChatML

### 2. Data Preprocessing (`scripts/preprocess_sft_pipeline.py`)

Полнофункциональный пайплайн предобработки данных.

**Этапы обработки:**

1. **Ingest** - Загрузка сырых данных
   - Поддержка множественных форматов (case/article, messages, title/content)
   - Автоматическое определение кодекса из имени файла
   - Группировка по статьям для предотвращения утечек

2. **Normalize** - Нормализация формата
   - Приведение к структуре `messages` (ChatML)
   - Добавление system prompt
   - Валидация структуры

3. **Clean** - Очистка текста
   - Удаление HTML-тегов и атрибутов
   - Нормализация пробелов и переносов строк
   - Удаление артефактов (undefined, null)

4. **Quality Filters** - Фильтры качества
   - Проверка минимальной длины (user: 5 символов, assistant: 10 символов)
   - Обнаружение повторяющихся символов
   - Проверка на обрывы текста
   - Максимальная длина: 50000 символов

5. **Deduplication** - Дедупликация
   - Exact duplicates по MD5 хэшу
   - Near-duplicates по похожести (Jaccard similarity > 95%)

6. **Split** - Разделение данных
   - Group split по `group_id` (статьям)
   - Стратификация по кодексам
   - Пропорции: 80% train / 10% val / 10% test

**Использование:**
```bash
python scripts/preprocess_sft_pipeline.py \
    --raw-dir data/raw \
    --output-dir data/processed/sft_datasets \
    --train-ratio 0.8 \
    --val-ratio 0.1 \
    --test-ratio 0.1 \
    --seed 42
```

**Выходные файлы:**
- `train.jsonl` - Обучающий набор
- `val.jsonl` - Валидационный набор
- `test.jsonl` - Тестовый набор
- `split_manifest.json` - Манифест разделения для воспроизводимости
- `preprocessing_report.json` - Отчет о предобработке

### 3. RAG System (`src/legal_rag/rag_system.py`)

Система семантического поиска по правовым документам.

**Компоненты:**
- **Эмбеддер**: `intfloat/multilingual-e5-large-instruct`
- **Векторное хранилище**: ChromaDB (PersistentClient)
- **Чанкинг**: Автоматическое разбиение статей > 2500 символов
- **Overlap**: 400 символов между чанками

**Функции:**

#### `build_vector_db(articles_path, db_path)`
Создает векторную базу данных из статей.

**Параметры:**
- `articles_path`: Путь к JSON файлу со статьями
- `db_path`: Путь к директории ChromaDB

**Метаданные:**
- `article_url`: URL статьи
- `article_title`: Название статьи
- `chunk_start`: Начало чанка в статье
- `chunk_end`: Конец чанка в статье
- `legal_code`: Код кодекса (GK, NK, TK, JK, UK)

#### `search_legal_context(query, db_path, top_k)`
Поиск релевантных статей по запросу.

**Параметры:**
- `query`: Текстовый запрос
- `db_path`: Путь к базе данных
- `top_k`: Количество результатов (по умолчанию 5)

**Возвращает:**
Список словарей с ключами:
- `text`: Текст статьи/чанка
- `article_title`: Название статьи
- `article_url`: URL статьи
- `legal_code`: Код кодекса

**Использование:**
```python
from src.legal_rag.rag_system import search_legal_context

results = search_legal_context(
    "Какие права имеет работник при увольнении?",
    db_path="./chroma_legal_db",
    top_k=5
)

for result in results:
    print(f"Статья: {result['article_title']}")
    print(f"Кодекс: {result['legal_code']}")
    print(f"Текст: {result['text'][:200]}...")
```

### 4. Fine-tuning (`src/legal_rag/finetune_qlora_4b.py`)

Универсальный скрипт для fine-tuning с поддержкой двух бэкендов.

#### MLX Backend (Apple Silicon)

**Особенности:**
- Использует MLX-LM для эффективного обучения
- Поддержка 8-bit quantization
- Автоматическое разбиение длинных последовательностей
- Gradient checkpointing для экономии памяти

**Параметры:**
- `--iters`: Количество итераций (по умолчанию 2000)
- `--batch`: Размер батча (по умолчанию 1)
- `--rank`: LoRA rank (по умолчанию 16)
- `--lora_alpha`: LoRA alpha (по умолчанию 2 * rank)
- `--mlx_effective_max_seq_length`: Эффективная длина последовательности (по умолчанию 10240)
- `--mlx_grad_checkpoint`: Включить gradient checkpointing
- `--mlx_chunk_long_sequences`: Разбивать длинные последовательности

**Пример:**
```bash
python src/legal_rag/finetune_qlora_4b.py \
    --data data/processed/sft_datasets/train.jsonl \
    --model Vikhrmodels/QVikhr-3-8B-Instruction-MLX_8bit \
    --output models/legal-assistant \
    --iters 2000 \
    --batch 1 \
    --rank 16 \
    --lr 2e-5 \
    --max_seq_length 12032 \
    --mlx_effective_max_seq_length 10240
```

#### CUDA Backend (NVIDIA GPU)

**Особенности:**
- Использует Transformers + PEFT (QLoRA)
- Поддержка Unsloth для ускорения (до 2.5× быстрее)
- 4-bit quantization через bitsandbytes
- 8-bit optimizers для экономии памяти

**Параметры:**
- `--epochs`: Количество эпох (по умолчанию 3)
- `--batch`: Размер батча (по умолчанию 4)
- `--rank`: LoRA rank (по умолчанию 64)
- `--gradient_checkpointing`: Включить gradient checkpointing
- `--save_steps`: Частота сохранения checkpoint'ов

**Пример:**
```bash
python src/legal_rag/finetune_qlora_4b.py \
    --data data/processed/sft_datasets/train.jsonl \
    --model mistralai/Mistral-7B-Instruct-v0.3 \
    --output models/legal-assistant \
    --epochs 3 \
    --batch 4 \
    --rank 64 \
    --lr 2e-4 \
    --max_seq_length 2048 \
    --gradient_checkpointing
```

**Поддержка форматов данных:**
- JSONL (рекомендуется): Одна запись на строку
- JSON: Массив объектов
- Поддержка gzip сжатия (.jsonl.gz)

**Автоматическое определение формата:**
- Поддержка legacy форматов (case/article, question/answer)
- Нормализация к формату `messages`

### 5. Dataset Health Check (`scripts/check_health_datasets.py`)

Проверка качества и целостности датасетов.

**Проверки:**
- Структура данных (формат `messages`)
- Длины сообщений (user, assistant, system)
- Дубликаты внутри датасета
- Утечки данных между train/test/val
- Наличие шума (HTML теги, undefined, null)

**Использование:**
```bash
python scripts/check_health_datasets.py \
    --train data/processed/sft_datasets/train.jsonl \
    --test data/processed/sft_datasets/test.jsonl \
    --val data/processed/sft_datasets/val.jsonl \
    --report-name ds_sanity_report.json
```

**Выходные данные:**
- JSON отчет с детальной статистикой
- Таблица в консоли с краткой сводкой

### 6. Utilities

#### `scripts/avg_article_length.py`
Вычисление статистики по длине статей.

```bash
python scripts/avg_article_length.py --dataset data/processed/enhanced_balanced_legal_dataset.json
```

#### `src/legal_rag/label_articles.py`
Добавление меток кодексов к статьям.

```bash
python src/legal_rag/label_articles.py
```

---

## 💡 Примеры использования

### Пример 1: Полный пайплайн от сбора до обучения

```bash
# 1. Сбор данных
python src/scrapers/smart-scraper.py

# 2. Обработка
python src/scrapers/process_dataset.py

# 3. Предобработка для обучения
python scripts/preprocess_sft_pipeline.py \
    --raw-dir data/raw \
    --output-dir data/processed/sft_datasets

# 4. Проверка качества
python scripts/check_health_datasets.py \
    --train data/processed/sft_datasets/train.jsonl \
    --test data/processed/sft_datasets/test.jsonl \
    --val data/processed/sft_datasets/val.jsonl

# 5. Обучение модели (Apple Silicon)
python src/legal_rag/finetune_qlora_4b.py \
    --data data/processed/sft_datasets/train.jsonl \
    --output models/legal-assistant
```

### Пример 2: Использование RAG системы

```python
from src.legal_rag.rag_system import build_vector_db, search_legal_context

# Создание базы данных
build_vector_db(
    articles_path="data/processed/articles_cleaned.json",
    db_path="./chroma_legal_db"
)

# Поиск статей
results = search_legal_context(
    "Какие права имеет работник при увольнении?",
    db_path="./chroma_legal_db",
    top_k=5
)

for i, result in enumerate(results, 1):
    print(f"\n{i}. {result['article_title']}")
    print(f"   Кодекс: {result['legal_code']}")
    print(f"   URL: {result['article_url']}")
    print(f"   Текст: {result['text'][:300]}...")
```

### Пример 3: Использование обученной модели

```python
from transformers import AutoTokenizer, AutoModelForCausalLM
from peft import PeftModel

# Загрузка модели и адаптеров
base_model = AutoModelForCausalLM.from_pretrained("mistralai/Mistral-7B-Instruct-v0.3")
model = PeftModel.from_pretrained(base_model, "models/legal-assistant")
tokenizer = AutoTokenizer.from_pretrained("models/legal-assistant")

# Генерация ответа
messages = [
    {"role": "system", "content": "Ты - юридический ассистент, который помогает разбираться в российском законодательстве."},
    {"role": "user", "content": "Какая статья ГК РФ регулирует договоры?"}
]

inputs = tokenizer.apply_chat_template(messages, return_tensors="pt")
outputs = model.generate(inputs, max_new_tokens=256, temperature=0.7)
response = tokenizer.decode(outputs[0], skip_special_tokens=True)
print(response)
```

---

## 📄 Формат данных

### Входной формат (сырые данные)

Поддерживаются различные форматы:

#### Формат 1: case/article
```json
{
  "case": "Могут ли государственные органы выступать участниками гражданских правоотношений?",
  "article": "Гражданское законодательство определяет правовое положение участников..."
}
```

#### Формат 2: messages (ChatML)
```json
{
  "messages": [
    {"role": "user", "content": "Вопрос..."},
    {"role": "assistant", "content": "Ответ..."}
  ]
}
```

#### Формат 3: title/content
```json
{
  "title": "Статья 1. Название статьи",
  "content": "Текст статьи..."
}
```

### Выходной формат (для обучения)

Все данные нормализуются к формату ChatML:

```json
{
  "messages": [
    {
      "role": "system",
      "content": "Ты - юридический ассистент, который помогает разбираться в российском законодательстве."
    },
    {
      "role": "user",
      "content": "Какая статья из Гражданского кодекса РФ регулирует договоры?"
    },
    {
      "role": "assistant",
      "content": "Статья 420. Понятие договора. Договором признается соглашение двух или нескольких лиц..."
    }
  ]
}
```

### Формат статей для RAG

```json
{
  "url": "https://www.zakonrf.info/gk/420/",
  "title": "ГК РФ. Статья 420. Понятие договора",
  "content": "Договором признается соглашение двух или нескольких лиц..."
}
```

---

## ⚙️ Конфигурация и параметры

### Параметры LoRA

#### MLX (по умолчанию)
```python
{
    "rank": 16,
    "lora_alpha": 32,  # 2 * rank
    "dropout": 0.05,
    "max_seq_length": 12032,
    "effective_max_seq_length": 10240
}
```

#### CUDA (по умолчанию)
```python
{
    "rank": 64,
    "lora_alpha": 32,
    "dropout": 0.05,
    "target_modules": [
        "q_proj", "k_proj", "v_proj", "o_proj",
        "gate_proj", "up_proj", "down_proj"
    ]
}
```

### Параметры обучения

#### MLX
- `batch_size`: 1 (консервативно для больших моделей)
- `learning_rate`: 2e-5
- `iters`: 2000
- `save_steps`: 200
- `logging_steps`: 200

#### CUDA
- `batch_size`: 4
- `gradient_accumulation_steps`: 8
- `learning_rate`: 2e-4
- `epochs`: 3
- `save_steps`: 200

### Параметры предобработки

```python
MIN_USER_LENGTH = 5  # Минимальная длина user сообщения
MIN_ASSISTANT_LENGTH = 10  # Минимальная длина assistant сообщения
MAX_TEXT_LENGTH = 50000  # Максимальная общая длина
TRAIN_RATIO = 0.8  # Доля train данных
VAL_RATIO = 0.1  # Доля val данных
TEST_RATIO = 0.1  # Доля test данных
```

### Параметры RAG

```python
CHUNK_SIZE = 2500  # Размер чанка для длинных статей
CHUNK_OVERLAP = 400  # Перекрытие между чанками
MIN_CHUNK_LENGTH = 200  # Минимальная длина чанка
BATCH_SIZE = 100  # Размер батча для добавления в ChromaDB
```

---

## 🐛 Решение проблем

### Проблема: MLX не найден на Apple Silicon

```bash
pip install mlx mlx-lm
```

### Проблема: CUDA недоступна

Убедитесь, что установлен правильный PyTorch с CUDA:
```bash
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
```

Проверка:
```python
import torch
print(torch.cuda.is_available())  # Должно быть True
```

### Проблема: Out of Memory при обучении

**Для MLX:**
- Уменьшите `--mlx_effective_max_seq_length` (например, до 8192)
- Уменьшите `--batch` до 1
- Включите `--mlx_grad_checkpoint`

**Для CUDA:**
- Уменьшите `--batch`
- Увеличьте `--gradient_accumulation_steps`
- Используйте `--gradient_checkpointing`
- Используйте меньшую модель

### Проблема: Ошибка импорта модулей

Убедитесь, что вы находитесь в корневой директории проекта:
```bash
cd /path/to/legal_nlp
export PYTHONPATH="${PYTHONPATH}:$(pwd)"
```

### Проблема: Файлы данных не найдены

Проверьте структуру директорий:
```bash
mkdir -p data/raw data/processed data/reports
```

### Проблема: ChromaDB ошибки

Удалите старую базу данных и пересоздайте:
```bash
rm -rf ./chroma_legal_db
python src/legal_rag/rag_system.py --build
```

---

## 📁 Структура проекта

```
legal_nlp/
├── data/
│   ├── raw/                          # Сырые данные со скраперов
│   │   ├── actual_gk_150.json
│   │   ├── actual_nk_150.json
│   │   └── ...
│   ├── processed/                    # Обработанные данные
│   │   ├── sft_datasets/            # Датасеты для обучения
│   │   │   ├── train.jsonl
│   │   │   ├── val.jsonl
│   │   │   ├── test.jsonl
│   │   │   ├── split_manifest.json
│   │   │   └── preprocessing_report.json
│   │   ├── articles_cleaned.json
│   │   └── ...
│   └── reports/                      # Отчеты о качестве данных
│       └── ds_sanity_report.json
│
├── models/                           # Обученные модели и адаптеры
│   ├── legal-assistant-mlx/
│   └── legal-assistant-cuda/
│
├── scripts/                          # Утилиты и скрипты
│   ├── preprocess_sft_pipeline.py   # Пайплайн предобработки
│   ├── check_health_datasets.py     # Проверка качества датасетов
│   └── avg_article_length.py        # Статистика по статьям
│
├── src/
│   ├── legal_rag/
│   │   ├── __init__.py
│   │   ├── rag_system.py            # RAG система с ChromaDB
│   │   ├── finetune_qlora_4b.py    # Fine-tuning скрипт (MLX/CUDA)
│   │   └── label_articles.py       # Разметка статей
│   │
│   └── scrapers/
│       ├── __init__.py
│       ├── base.py                  # Базовый класс для стратегий
│       ├── smart-scraper.py        # Скрапер для actual.pravo.gov.ru
│       ├── zakonrf.py              # Скрапер для zakonrf.info
│       ├── sudact.py               # Скрапер для sudact.ru
│       └── process_dataset.py      # Обработка собранных данных
│
├── pyproject.toml                   # Зависимости проекта
├── Makefile                         # Команды для разработки (make test, make lint и т.д.)
├── README.md                        # Этот файл
├── architecture.md                  # Архитектура системы с диаграммами
└── tests/                           # Unit-тесты проекта
    ├── test_scrapers.py
    ├── test_preprocessing.py
    └── test_rag_system.py
```

---

## 📝 Лицензия

[Укажите лицензию проекта]

## 👥 Авторы

- **Anton Amadeus** (amadey1110@gmail.com)

## 🙏 Благодарности

- **Saiga-Mistral** за базовую модель для русского языка
- **Hugging Face** за экосистему Transformers
- **MLX team** за эффективное обучение на Apple Silicon
- **ChromaDB** за векторное хранилище

## 🧪 Тестирование

Проект включает unit-тесты для основных компонентов.

### Установка зависимостей для тестирования

```bash
# Через Makefile (рекомендуется)
make install-dev

# Или напрямую через pip
pip install -e ".[dev]"
# или
pip install pytest pytest-cov
```

### Запуск тестов

#### Через Makefile (удобный способ)

```bash
# Все тесты
make test

# Тесты с покрытием кода
make test-cov

# Проверка кода линтером
make lint

# Форматирование кода
make format

# Очистка временных файлов
make clean

# Показать все доступные команды
make help
```

#### Напрямую через pytest

```bash
# Все тесты
pytest tests/ -v

# Конкретный файл
pytest tests/test_scrapers.py -v

# С покрытием кода
pytest tests/ --cov=src --cov=scripts --cov-report=html
```

### Структура тестов

- `tests/test_scrapers.py` - Тесты для web scrapers (BaseStrategy, ZakonRFStrategy, SudactStrategy)
- `tests/test_preprocessing.py` - Тесты для пайплайна предобработки данных
- `tests/test_rag_system.py` - Тесты для RAG системы
- `tests/conftest.py` - Конфигурация pytest и общие фикстуры

Подробнее: [tests/README.md](tests/README.md)

## 📚 Дополнительные ресурсы

- [Архитектура системы](architecture.md) - Детальная архитектура с Mermaid диаграммами
- [Тесты](tests/README.md) - Unit-тесты проекта
- [План реализации](implementation_plan.md.resolved)

---

**Версия документа**: 2.0  
**Последнее обновление**: 2025
